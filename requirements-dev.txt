# Development and test dependencies.
#
#   pip install -r requirements-dev.txt
#
# The unit suite needs no database and no ODBC driver, but it does import the collector modules,
# so the runtime requirements are included here too.

-r requirements.txt

pytest>=7.4
pytest-cov>=4.1
