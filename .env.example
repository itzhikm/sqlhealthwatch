# Secrets referenced from YAML as `env:NAME`. Copy to .env (gitignored) and fill in.
REPO_PW=
# Shared SQL-auth password for the pyspark_user login used by every server in servers.yml.
SQL_MONITOR_PW=
SMTP_PW=
TEAMS_WEBHOOK=
SLACK_WEBHOOK=
